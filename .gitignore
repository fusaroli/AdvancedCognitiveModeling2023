simdata/
simmodels/
.Rproj.user
.Rhistory
.RData
.Ruserdata
.DS_Store
connect_sim_data.csv
connect_sim_estimates.csv
models/
/.quarto/
**/*.quarto_ipynb

# Compiled Stan executables (regenerated on render; .stan sources are tracked)
stan/*
!stan/*.stan
!stan/.gitkeep
outputs/*
!outputs/*.stan
!outputs/.gitkeep

# Bookdown leftovers (no longer regenerated under Quarto)
_bookdown_files/
series_cache/
series_files/

# Quarto / knitr build intermediates at project root
# (docs/<chapter>_files/ is the published copy and stays tracked)
/*_cache/
/*_files/

# Misc root-level build artifacts
/tikz*.log
/prior_posterior*.pdf
